<%= simple_form_for @garin do |f| %>
	<%= f.input :name, label: "שם" %>
	<%= f.input :start_money if action_name == "new", label: "כסף התחלתי" %>
	<%= f.input :start_day, collection: [10, 12, 15], label: "יום תחילת מחזור כלכלי" %>
	<%= f.button :submit, "שליחה" %>
<% end %>
