

<%= simple_form_for @garin do |f| %>
	<%= f.input :name %>
	<%= f.input :start_money if action_name == "new" %>
	<%= f.input :start_day, collection: [10, 12, 15] %>
	<%= f.button :submit %>
<% end %>
