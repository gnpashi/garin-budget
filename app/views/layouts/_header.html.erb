
<header class="flex justify-between p-3" >
  <div class="content flex flex-col  justify-between">
    <div class="text-2xl">
      <%= link_to "תקציב #{current_user.garin.name}", root_path, class:"title" if user_signed_in? %>
    </div>
    <div class="text-xl">
      <%= @garin.current_period.heb_month %>
    </div>
    <div class="">
      <span class="text-2xl"><%= @garin.current_period.money %>/</span>
      <span class="font-bold text-3xl"><%= @garin.current_period.current_money %></span>
    </div>

    <div class="">

    </div>
  </div>
</header>


<div class="hamburger block lg:hidden">
  <button class="flex items-center" type="button" name="button">
    <%= image_tag "nav.svg" %>
  </button>
</div>
